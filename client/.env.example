# Local development uses the CRA proxy when this is unset. Set it only when
# developing against a separately hosted API.
REACT_APP_API_URL=http://localhost:5000

# Vercel production: set this in the Vercel Production environment as well.
# REACT_APP_API_URL=https://sas-academy-1ruh.onrender.com
