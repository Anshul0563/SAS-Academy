# TODO

- [x] Add `logo.png` and `favicon.png` already exist in `client/public/`.
- [x] Update sidebar in `client/src/components/Layout.js` to show `logo.png`.
- [x] Keep behavior responsive (no sidebar shown during exam mode).
- [x] Run frontend checks (npm test) to ensure no missing asset errors.


